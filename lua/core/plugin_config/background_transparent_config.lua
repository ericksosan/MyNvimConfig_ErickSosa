require("transparent").setup({ -- Optional, you don't have to run setup.

  extra_groups = {
    "BufferLineTabClose",
    "BufferLineBufferSelected",
    "BufferLineFill",
    "BufferLineBackground",
    "BufferLineBufferSeparator",
    "BufferLineBufferIndicatorSelected",
  }, 
  exclude_groups = {
  },
})
